import {
	START_TRACKING,
	STOP_TRACKING,
	PAUSE_TRACKING
} from '../actions/TrackingActions';

const initialState = {
	
}

export default function auth (state = initialState, action) {
  switch (action.type) {
	  default:
		return state;
  }
}